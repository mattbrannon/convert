import { cmyk, hex, hsl, hsv, hwb, lch, rgb } from './colors';
import { radian, gradian, degree, turn } from './angles';

export default {
  cmyk,
  hex,
  hsl,
  hsv,
  hwb,
  lch,
  rgb,
  radian,
  gradian,
  degree,
  turn,
};

export { cmyk, hex, hsl, hsv, hwb, lch, rgb, radian, gradian, degree, turn };
